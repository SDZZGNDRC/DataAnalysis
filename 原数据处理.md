# 工具脚本说明

### `recategory.py`
首先确保某一个月份的所有原始数据都已经放置在同一个目录下，然后调用下面的命令：
```bash
python ./recategory.py "E:\datapool\2025-03-.*" E:\tmp\2025-03
```
然后，调用：
```bash
python ./grayscale_pack.py --workers 6 E:\tmp\2025-03\
```
这样，一个月份的所有数据就打包好了，后续研究中，需要哪类数据就复制对应的文件夹，然后调用命令解压：
```bash
python ./unzip.py E:\tmp\new_dir\Books\OKX-Books-BTC-USDT-400\ --overwrite
```
这样，所有7z文件就已经解压为json文件，然后，调用：
```bash
python ./crypto_data_aggregator.py --dir E:\tmp\new_dir\Books\OKX-Books-BTC-USDT-400 --output E:\tmp\new_dir\Books\OKX-Books-BTC-USDT-400
```
这样，原始的json文件就会被聚合为parquet文件。

接下来，还需要处理这些parquet文件中的时间戳overlapped现象，调用：
```bash
python ./resort_parquet.py --dir E:\tmp\new_dir\Books\OKX-Books-BTC-USDT-400
```
至此，对于非books类型的原始数据的处理就已经完成；而对于books类型的数据，我们还需要进一步调用：
```bash
python ./tail_books.py --dir E:\tmp\new_dir\Books\OKX-Books-BTC-USDT-400
```

以后，我们可以调用其他脚本生成指标，比如：
```bash
python ./gen_BLCSI.py --dir "E:\tmp\new_dir\Books\OKX-Books-BTC-USDT-400" --output "E:\tmp\OKX-BLCSI-BTC-USDT"
```

### check_seqId.py
检查原始7z文件或聚合后的parquet文件中`seqId`与`prevSeqId`的连续性，识别出断链（gaps）和连续段（segments）。支持多进程并行处理。

- **对于7z文件（原始JSON数据）**：使用`--raw`模式，会解析每个JSON条目中的`seqId`和`prevSeqId`，合并相邻文件中的连续段，并输出gaps和segments的CSV文件。
- **对于parquet文件**：默认模式，检查每个row group内以及跨文件、跨row group的`seqId`连续性，报告不匹配的错误。

**用法示例**：

检查parquet文件：
```bash
python check_seqId.py --dir E:\tmp\new_dir\Books\OKX-Books-BTC-USDT-400
```

检查7z文件并输出gaps和segments：
```bash
python check_seqId.py --dir E:\datapool\2025-03 --raw --output seqId_gaps.csv --show_seg seqId_segments.csv
```

**参数说明**：
- `--dir`：一个或多个包含待检查文件的目录（可递归搜索）。
- `--num-processes`：并行进程数，默认为CPU核心数。
- `--raw`：指定输入为7z文件（否则视为parquet）。
- `--output`：gaps输出CSV文件路径（仅raw模式）。
- `--show_seg`：所有segments输出CSV文件路径（仅raw模式）。

### find_snapshot_files.py
查找包含 snapshot 的 OKX 数据文件（.7z 或 .json 格式），支持多进程并行处理。可以输出包含 snapshot 的文件列表，或生成包含每个 snapshot 详细信息的 CSV 文件。

- **对于 .7z 文件**：自动解压到临时目录，检查内部的 JSON 文件，处理完成后删除临时文件。
- **对于 .json 文件**：直接读取检查。

**用法示例**：

仅列出包含 snapshot 的文件：
```bash
python find_snapshot_files.py E:\tmp\OKX-Books-BTC-USDT-400-2025-03\OKX-Books-BTC-USDT-400
```

使用多进程并输出 snapshot 详细信息到 CSV：
```bash
python find_snapshot_files.py E:\datapool\2025-03 --processes 8 --output-csv snapshot_details.csv
```

**参数说明**：
- `path`：文件路径模式，支持通配符或目录（可递归搜索 .7z 和 .json 文件）。
- `--processes`：并行进程数，默认为 CPU 核心数。
- `--output-csv`：保存 snapshot 详细信息的 CSV 文件路径。如果指定，则输出每个 snapshot 的记录（文件路径、索引、时间戳、总条目数、相对位置）；否则仅输出文件路径列表。

### reconstruct_segments.py
从多个7z文件中重建连续段（segments），基于`seqId`和`prevSeqId`的连续性，并使用位图（bitmap）记录每个段覆盖了哪些文件。支持多进程并行处理，输出包含起始/结束seqId、时间戳、持续时间及覆盖文件列表的CSV文件。

- **对于7z文件（原始JSON数据）**：解析每个JSON条目中的`seqId`和`prevSeqId`，合并相邻文件中的连续段，并生成全局的连续段列表。
- **位图覆盖**：每个段用一个整数位图表示，其中每一位对应一个输入文件，用于快速判断该段跨越了哪些文件。
- **时间区间验证**：自动检查最终段的时间戳区间是否重叠，确保数据一致性。

**用法示例**：

重建指定目录下的所有7z文件：
```bash
python reconstruct_segments.py --dir E:\datapool\2025-03
```

指定多个目录并使用8个进程：
```bash
python reconstruct_segments.py --dir E:\datapool\2025-03 E:\datapool\2025-04 --num-processes 8
```

自定义输出文件路径：
```bash
python reconstruct_segments.py --dir E:\datapool\2025-03 --output my_segments.csv
```

**参数说明**：
- `--dir`：一个或多个包含待处理7z文件的目录（可递归搜索`.7z`文件）。必需参数。
- `--num-processes`：并行进程数，默认为CPU核心数。
- `--output`：输出CSV文件路径，默认为`reconstructed_segments.csv`。

**输出CSV列说明**：
- `prevSeqId`：段的起始前序seqId（即该段第一个条目的prevSeqId）。
- `seqId`：段的结束seqId（即该段最后一个条目的seqId）。
- `startSeqId`：段的起始seqId（即该段第一个条目的seqId）。
- `start_timestamp`：段内最早时间戳（毫秒）。
- `start_datetime`：格式化的起始时间（yyyy-mm-dd hh:mm）。
- `end_timestamp`：段内最晚时间戳（毫秒）。
- `end_datetime`：格式化的结束时间。
- `duration_hours`：段持续时间（小时）。
- `covered_files`：覆盖的文件列表，以分号分隔。

### concat_books_7z.py
从多个7z文件中提取订单簿（Books）数据，并基于`seqId`和`prevSeqId`的链式关系重建连续的数据序列。该脚本会从指定的起始seqId开始，沿着`prevSeqId`链接收集数据点，直到达到结束时间戳，最终输出一个包含收集到的数据点的JSON文件（可选压缩为7z格式）。支持多进程并行读取文件，适用于大规模数据拼接。

**用法示例**：

从多个7z文件中提取数据，指定起始seqId和时间区间：
```bash
python concat_books_7z.py file1.7z file2.7z --startSeqId 1000 --start_timestamp 1740760581603 --end_timestamp 1740783439607 --output concatenated_books.json
```

使用分号分隔的文件列表（适用于大量文件）：
```bash
python concat_books_7z.py "file1.7z;file2.7z;file3.7z" --startSeqId 1000 --start_timestamp 1740760581603 --end_timestamp 1740783439607
```

指定并行进程数：
```bash
python concat_books_7z.py *.7z --startSeqId 1000 --start_timestamp 1740760581603 --end_timestamp 1740783439607 --num-processes 8
```

**参数说明**：
- `files`：一个或多个7z文件路径（支持通配符`*.7z`），若文件数量过多可使用分号分隔的字符串。
- `--startSeqId`：起始seqId（必需），脚本将从该seqId开始收集数据链。
- `--start_timestamp`：起始时间戳（毫秒，必需），用于时间区间过滤（实际过滤以`end_timestamp`为准）。
- `--end_timestamp`：结束时间戳（毫秒，必需），丢弃时间戳大于此值的数据点。
- `--output`：输出JSON文件名，默认为`concatenated_books.json`。脚本会自动根据数据前缀和时间戳生成最终文件名（格式：`{prefix}-{min_ts}-{max_ts}.json`），该参数仅用于中间文件命名。
- `--num-processes`：并行工作进程数，默认为CPU核心数。

**输出说明**：
1. 脚本会生成一个JSON文件，其结构为：
   ```json
   {
     "elapsedTime": [min_timestamp, max_timestamp],
     "data": [ ... ], // 收集到的数据点列表
     "extend": { "sorted": false, "true_elapsedTime": true }
   }
   ```
2. 随后自动调用7z（或py7zr）将该JSON文件压缩为同名的`.7z`文件，并删除原始的JSON文件。
3. 最终输出文件命名规则：基于第一个输入文件解析出的数据前缀（如`OKX-Books-BTC-USDT-400`）以及实际收集到的时间戳区间，生成`{prefix}-{min_ts}-{max_ts}.7z`。

### find_trades_by_timestamp.py
在给定的时间戳范围内查找包含交易数据（Trades）的7z文件，并可选地将选中的文件合并为一个新的7z文件。该脚本会递归扫描源目录中的所有.7z文件，提取每个文件的时间戳范围（最小和最大时间戳），然后根据用户提供的时间范围选择覆盖该范围的最小文件集合。支持多进程并行处理，适用于大规模数据检索。

**用法示例**：

查找覆盖时间戳区间 [1740760400495, 1740785409066] 的交易数据文件，并将结果保存为CSV：
```bash
python find_trades_by_timestamp.py 1740760400495 1740785409066 E:\datapool\ --save-csv --output found_trades_files.csv
```

查找相同区间并直接合并文件（默认行为）：
```bash
python find_trades_by_timestamp.py 1740760400495 1740785409066 E:\datapool\ --num-processes 8
```

**参数说明**：
- `startTimestamp`：起始时间戳（毫秒，必需），目标时间区间的开始。
- `endTimestamp`：结束时间戳（毫秒，必需），目标时间区间的结束。
- `source_dir`：源目录路径（必需），包含待扫描的7z文件（支持子目录递归）。
- `--output`：输出CSV文件名（仅当`--save-csv`时有效），默认为`found_trades_files.csv`。
- `--num-processes`：并行工作进程数，默认为CPU核心数。
- `--save-csv`：如果指定，则将选中的文件列表保存为CSV；否则，将选中的文件合并为一个新的7z文件。

**输出说明**：
1. 若指定`--save-csv`，脚本会生成一个CSV文件，包含以下列：
   - `file_index`：文件序号（按文件名排序）
   - `file_path`：文件绝对路径
   - `start_timestamp`：该文件的最小时间戳
   - `end_timestamp`：该文件的最大时间戳

2. 若未指定`--save-csv`（默认行为），脚本会执行以下步骤：
   - 从选中的文件中提取所有交易数据条目，合并为一个JSON对象。
   - 生成一个JSON文件，其结构为：
     ```json
     {
       "elapsedTime": [overall_min_ts, overall_max_ts],
       "data": [ ... ]  // 合并后的交易数据列表
     }
     ```
   - 随后自动调用py7zr将该JSON文件压缩为同名的`.7z`文件，并删除原始的JSON文件。
   - 最终输出文件命名规则：基于第一个输入文件解析出的数据前缀（如`OKX-Trades-BTC-USDT`）以及实际合并的时间戳区间，生成`{prefix}-{min_ts}-{max_ts}.7z`。

**注意事项**：
- 脚本假设每个.7z文件中仅包含一个JSON文件（符合OKX数据导出格式）。
- 时间戳提取依赖于数据模式`item['data'][0]['ts']`，若数据结构变化可能导致提取失败。
- 若存在多个文件完全覆盖目标时间范围，脚本会给出提示，但仍会按连续范围选择文件集合。
### merge_segments.py
基于时间间隔合并连续的段（segments），以减少段数量并生成更长的连续数据区间。该脚本读取由`reconstruct_segments.py`生成的CSV文件，筛选出`prevSeqId == -1`的段（即起始段），然后根据用户指定的最大时间间隔（`--max-gap`）将相邻的段合并。合并后的段会更新其时间范围、覆盖文件列表和持续时间，输出为新的CSV文件。

**用法示例**：

合并段，设置最大间隔为1小时：
```bash
python merge_segments.py --max-gap 1h
```

指定输入和输出文件：
```bash
python merge_segments.py --input reconstructed_segments.csv --output merged_segments.csv --max-gap 1m --max-duration 100h
```

**参数说明**：
- `--input`：输入CSV文件路径，默认为`reconstructed_segments.csv`。该文件应包含`prevSeqId`、`seqId`、`start_timestamp`、`end_timestamp`、`covered_files`等列。
- `--output`：输出CSV文件路径，默认为`merged_segments.csv`。
- `--max-gap`：最大时间间隔（必需），用于决定两个段是否可以合并。格式为数字加单位，支持的单位：`s`（秒）、`m`（分钟）、`h`（小时）、`d`（天）。例如`1h`表示1小时，`30m`表示30分钟。

**输出说明**：
1. 脚本会生成一个CSV文件，包含以下列：
   - `prevSeqId`：合并后段的起始prevSeqId（保持为-1）。
   - `seqId`：合并后段的结束seqId（取自最后一个被合并的段）。
   - `start_timestamp`：合并后段的起始时间戳（毫秒）。
   - `start_datetime`：格式化的起始时间。
   - `end_timestamp`：合并后段的结束时间戳（毫秒）。
   - `end_datetime`：格式化的结束时间。
   - `duration_hours`：合并后段的持续时间（小时）。
   - `covered_files`：合并后段覆盖的所有文件路径，以分号分隔。

2. 脚本会在控制台输出处理统计信息，包括处理的段数量、合并次数以及最终段数量。

**注意事项**：
- 仅处理`prevSeqId == -1`的段，忽略其他段（因为它们可能是连续链中的中间段）。
- 合并逻辑：对于每个段，寻找与其时间间隔（当前段的开始时间减去候选段的结束时间）小于等于`max-gap`的已存在段，选择间隔最小的进行合并。如果找不到，则作为新段加入结果集。
- 合并时更新目标段的结束时间为当前段的结束时间（如果更大），并累加`covered_files`。
- 时间间隔允许负值（即重叠），重叠的段也会被合并。

### process_segments_to_npz.py

```bash
python process_segments_to_npz.py --csv merged_segments.csv --trades-dir E:\tmp\OKX-Trades-BTC-USDT --output-dir E:\output_npz --use-random-latency --require-trades --tmp-dir E:\tmp
```

### analyze_trades.py

分析OKX Trades 7z文件中的交易时间戳（ts）的最小值和最大值，并将结果输出到CSV文件中。

```bash
python analyze_trades.py <root_dir> [--output <output_file>]
```

- `<root_dir>`: 搜索OKX-Trades-*.7z文件的根目录
- `--output`: 输出CSV文件路径（默认: trades_ts_summary.csv）

# 其他有用的powershell命令

```psh
Get-ChildItem -Directory -Path "E:\datapool\" | Where-Object { $_.Name -match "^2025-03-([0-3][0-9])" } | Remove-Item -Recurse -Force

Get-ChildItem -Directory -Path "E:\tmp\" | Where-Object { $_.Name -match "^2025-04" } | Remove-Item -Recurse -Force

Get-ChildItem -Directory -Path "E:\datapool\" | Where-Object { $_.Name -match "^2025-05-([2-3][0-9])" } | Remove-Item -Recurse -Force
```

## 读取Parquet文件的Schema

```bash
pip install parquet-cli
parq file.parquet -s
```

## 寻找存在`snapshot`的文件

```bash
python find_snapshot_files.py E:\tmp\OKX-Books-BTC-USDT-400-2025-03\OKX-Books-BTC-USDT-400
```

## 生成`hftbacktest`的数据

```bash
# 注意，这行生成的是不带有trades数据的，仅有orderbook数据的npz文件
python hftbacktest_okx.py -o OKX-Books-BTC-USDT-400-2025-03-02.npz --feed-latency 200000000 --base-latency 100000000 E:\tmp\OKX-Books-BTC-USDT-400-2025-03\OKX-Books-BTC-USDT-400\2025-03-02

# 注意`latency-mu`不是指均值！！！！
python hftbacktest_okx.py -o OKX-HFT-BTC-USDT-2025-03-02.npz --feed-latency 200000000 --base-latency 100000000 --latency-mu -9.71 --latency-sigma 1.0 --use-random-latency E:\tmp\OKX-Trades-BTC-USDT-2025-03\OKX-Trades-BTC-USDT\2025-03-02 E:\tmp\OKX-Books-BTC-USDT-400-2025-03\OKX-Books-BTC-USDT-400\2025-03-02
```

## 生成ohlc数据

```bash
python hft_ohlc.py "OKX-HFT-BTC-USDT-2025-03-*.npz" -o "OKX-HFT-BTC-USDT-2025-03.csv" -i 1m
```


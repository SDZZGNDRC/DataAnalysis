# 背景

`hftbacktest`回测框架需要输入深度数据与交易数据，由于我收集到数据没有`localTs`，只有交易所时间戳，而且深度数据和交易数据是在不同的文件中存储的，仅能保证在各自的文件中是按照本地接收到的时间顺序排列的。
因此，如果要使用`hftbacktest`，就需要将深度数据和交易数据合并为一个文件。

对于本地时间戳缺失的问题，我的解决方案是人工合成本地时间戳，`localTs = exchangeTs + latency`, 其中
`latency`是一个随机变量，遵循`对数正态分布`。

以下讨论中，我们约定：
1. 深度数据是一个列表`D = [d1, d2, ..., dn]`，其中`di = (exchangeTs_i, data)`，`data`是深度数据的内容，`exchangeTs_i`是交易所时间戳，这个列表中的数据点是根据本地收到的时间顺序排列的，即`i < j`时，`di`是在`dj`之前收到的。由于网络延迟等原因，`exchangeTs_i`不一定小于`exchangeTs_j`。
2. 交易数据是一个列表`T = [t1, t2, ..., tm]`，其中`ti = (exchangeTs_i, data)`，`data`是交易数据的内容，`exchangeTs_i`是交易所时间戳，这个列表中的数据点也是根据本地收到的时间顺序排列的。
3. 我们需要合成一个新的列表`S = [s1, s2, ..., sk]`，其中每个`si`是深度数据或交易数据，其结构为`si = (exchangeTs_i, localTs_i, data)`并且`S`中的数据点是根据合成的本地时间戳`localTs`排序的。

## 约束

对于生成的`S`，我们需要满足以下约束：
1. **时间顺序一致性**：对于任意两个数据点`si`和`sj`，如果`i < j`，则`localTs_i <= localTs_j`。这保证了合成的本地时间戳是单调递增的。
2. **数据完整性**：所有的深度数据和交易数据都必须包含在`S`中，即`k = n + m`。
3. **随机延迟分布**：`latency`应符合对数正态分布，参数可以根据实际网络延迟情况进行调整。
4. **本地顺序不变性**：假设函数`filter_D(S)`按照`S`中的顺序过滤出所有深度数据点，得到`D'`，则`D'`的顺序应与原始深度数据`D`的顺序一致。同理，函数`filter_T(S)`按照`S`中的顺序过滤出所有交易数据点，得到`T'`，则`T'`的顺序应与原始交易数据`T`的顺序一致。

这些约束保证了总体的`latency`既在总体上符合对数正态分布，又保证了深度数据和交易数据在各自的本地顺序不变性。

## 初版合并方案

由于深度数据远比交易数据多，我们先对深度数据进行处理，然后将交易数据插入到合适的位置。

1. **生成本地时间戳**：对每个深度数据点`di`，生成一个符合`对数正态分布`的随机延迟`latency_i`，计算`localTs_i = exchangeTs_i + latency_i`。这样，我们得到一个新的列表`D' = [(exchangeTs_1, localTs_1, data_1), (exchangeTs_2, localTs_2, data_2), ..., (exchangeTs_n, localTs_n, data_n)]`,注意，由于需要遵循**本地顺序不变性**，我们需要确保`localTs_i`是单调递增的。如果生成的`localTs_i`不满足这个条件，我们可以通过调整`latency_i`来修正，确保`localTs_i > localTs_{i-1}`，只要总体上`latency_i`仍然符合对数正态分布即可。
2. **插入交易数据**：对于每个交易数据点`ti`，我们需要找到合适的位置将其插入到`D'`中。具体来说，我们需要找到`D'`中第一个满足`localTs_j >= exchangeTs_i + latency_i`的位置`j`，然后将`ti`插入到位置`j`之前。这里的`latency_i`同样是一个符合对数正态分布的随机变量。插入后，我们得到最终的列表`S`。同样，需要遵循**本地顺序不变性**，确保插入后的`S`中交易数据的顺序与原始交易数据`T`的顺序一致，为此，我们可以调整`latency_i`。


## 最终版合并方案

本方案采用**基于交易所时间戳的归并合并**策略。其核心思想是，公平地对待深度数据（D）和交易数据（T）两个独立的事件流，使用它们共有的、最客观的时间信息——**交易所时间戳 (`exchangeTs`)**——作为合并排序的主要依据。这种方法避免了将任一数据流作为“主干”而引入的潜在偏差，并且实现逻辑清晰、鲁棒。

此方案通过一个两阶段过程来满足所有约束：首先，通过类似“归并排序”的合并步骤来解决数据流的交叉排序问题，保证**数据完整性**和**本地顺序不变性**；然后，在合并后的统一事件流上生成随机延迟，并强制单调性，以满足**随机延迟分布**和**时间顺序一致性**的约束。

### 算法步骤

1.  **数据加载与预处理**
    *   **a.** 分别读取所有的深度数据文件（`Books`）和交易数据文件（`Trades`）。关键在于，必须先按文件名中包含的起始时间戳对文件列表进行排序，然后再依次读取文件内容。
    *   **b.** 将读取的数据分别存入两个大型`numpy`数组：深度数据数组`D`和交易数据数组`T`。经过此步骤，我们得到两个数组，其内部的事件各自严格遵守其原始的本地接收顺序。

2.  **基于交易所时间戳的归并**
    *   **a.** 初始化两个指针，`ptr_d = 0` 指向`D`的起始位置，`ptr_t = 0` 指向`T`的起始位置。同时创建一个新的空列表`M`，用于存放合并后的结果。
    *   **b.** 当`ptr_d`和`ptr_t`都未到达各自数组末尾时，循环比较 `D[ptr_d].exchangeTs` 和 `T[ptr_t].exchangeTs` 的值。
    *   **c.** 将`exchangeTs`较小的那个事件追加到`M`中，并将其对应的指针向后移动一位（`ptr_d++` 或 `ptr_t++`）。
    *   **d.** 如果循环结束后，某个数组仍有剩余事件（例如`D`已处理完，`T`还有剩余），则将该数组中所有剩余的事件直接依次追加到`M`的末尾。
    *   **结果**：此步骤完成后，我们得到一个统一的事件流`M`。这个流近似按照`exchangeTs`排序，但更重要的是，它**严格保证了**所有来自`D`的事件保持了其在`D`中的相对顺序，所有来自`T`的事件也同理。这就完美地满足了“**本地顺序不变性**”约束。

3.  **生成单调递增的本地时间戳**
    *   **a.** 初始化一个变量 `last_local_ts = 0`，用于记录前一个事件最终的本地时间戳。
    *   **b.** 遍历合并后的事件流`M`中的每一个事件`m_i`：
        *   i.  从设定的**对数正态分布**中抽取一个随机延迟样本 `latency_i`。
        *   ii. 计算一个候选本地时间戳 `candidate_localTs = m_i.exchangeTs + latency_i`。
        *   iii. 为保证本地时间戳的严格单调性，应用修正规则：`final_localTs = max(candidate_localTs, last_local_ts + ε)`。其中`ε`是一个极小的时间增量（如`1`纳秒），以确保时间戳不会重复。
        *   iv. 将最终的时间戳赋值给当前事件：`m_i.localTs = final_localTs`。
        *   v.  更新 `last_local_ts = final_localTs`，为处理下一个事件做准备。
    *   **结果**：此步骤完成后，`M`中的所有事件都有了一个满足“**时间顺序一致性**”的`localTs`。同时，`latency`的生成过程也基本遵循了“**随机延迟分布**”的约束（尽管`max`修正会对其产生轻微的左侧截断效应，但这是为满足单调性所必须的代价）。

4.  **最终处理与输出**
    *   **a.** 将处理完成的事件流`M`（现在可以称之为`S`）传递给`hftbacktest`框架提供的标准工具函数进行最后的整理。
    *   **b.** 调用 `correct_event_order` 函数。这一步至关重要，它会根据`exch_ts`和`local_ts`的排序，将每个原始记录拆分为`EXCH_EVENT`和`LOCAL_EVENT`，生成最终符合回测引擎格式要求的数据。
    *   **c.** （可选但推荐）调用 `validate_event_order` 函数，对生成的数据进行最终检查，确保没有时间顺序错误。
    *   **d.** 将最终生成的数据保存为`.npz`文件，供回测使用。


